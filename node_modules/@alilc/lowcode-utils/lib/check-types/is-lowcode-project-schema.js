"use strict";

exports.__esModule = true;
exports.isLowcodeProjectSchema = isLowcodeProjectSchema;
var _isComponentSchema = require("./is-component-schema");
var _isObject = require("../is-object");
function isLowcodeProjectSchema(data) {
  if (!(0, _isObject.isObject)(data)) {
    return false;
  }
  if (!('componentsTree' in data) || data.componentsTree.length === 0) {
    return false;
  }
  return (0, _isComponentSchema.isComponentSchema)(data.componentsTree[0]);
}