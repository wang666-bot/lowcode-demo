import * as React from 'react';
import './index.less';
interface NumberSetterProps {
    value: number;
    min: number;
    max: number;
    defaultValue: number;
    step: number | string;
    units: string;
    onChange: (val: number | null) => void;
    precision: number;
}
interface NumberSetterState {
    setterValue: number | null;
}
export default class NumberSetter extends React.PureComponent<NumberSetterProps, NumberSetterState> {
    static displayName: string;
    render(): React.JSX.Element;
}
export {};
