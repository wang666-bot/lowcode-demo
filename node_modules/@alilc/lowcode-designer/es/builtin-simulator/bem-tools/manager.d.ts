import { ComponentType } from 'react';
import { Designer } from '../../designer';
import { BuiltinSimulatorHost } from '../../builtin-simulator/host';
export declare type BemToolsData = {
    name: string;
    item: ComponentType<{
        host: BuiltinSimulatorHost;
    }>;
};
export declare class BemToolsManager {
    private designer;
    private toolsContainer;
    constructor(designer: Designer);
    addBemTools(toolsData: BemToolsData): void;
    removeBemTools(name: string): void;
    getAllBemTools(): BemToolsData[];
}
