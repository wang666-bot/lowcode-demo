import { Designer } from '../../designer';
export default class DragResizeEngine {
    private emitter;
    private dragResizing;
    private designer;
    constructor(designer: Designer);
    isDragResizing(): boolean;
    /**
     * drag reszie from
     * @param shell
     * @param direction n/s/e/w
     * @param boost (e: MouseEvent) => VE.Node
     */
    from(shell: Element, direction: string, boost: (e: MouseEvent) => any): () => void;
    onResizeStart(func: (e: MouseEvent, direction: string, node: any) => any): () => void;
    onResize(func: (e: MouseEvent, direction: string, node: any, moveX: number, moveY: number) => any): () => void;
    onResizeEnd(func: (e: MouseEvent, direction: string, node: any) => any): () => void;
    private getMasterSensors;
}
