import { INode } from '../../document';
/**
 * 获取离当前节点最近的可点击节点
 * @param currentNode
 * @param event
 */
export declare const getClosestClickableNode: (currentNode: INode | undefined | null, event: MouseEvent) => INode;
