function convertCharStr2CSS(ch) {
    var code = ch.charCodeAt(0).toString(16);
    while (code.length < 4) {
        code = "0".concat(code);
    }
    return "\\".concat(code);
}
module.exports = convertCharStr2CSS;
//# sourceMappingURL=convertCharStr.js.map