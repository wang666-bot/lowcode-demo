"use strict";

exports.__esModule = true;
exports.isLessThanVersion = void 0;
// 比较传入版本1 是否小于传入版本2，如果相等或者大于 都返回false
var isLessThanVersion = function isLessThanVersion(target, current, splitSign) {
  if (splitSign === void 0) {
    splitSign = '.';
  }
  var targetArr = target.split(splitSign);
  var currentArr = current.split(splitSign);
  var res = false;
  currentArr.some(function (i, idx) {
    if (!targetArr[idx]) {
      return true;
    }
    if (i > targetArr[idx]) {
      res = true;
      return true;
    } else if (i < targetArr[idx]) {
      res = false;
      return true;
    }
  });
  return res;
};
exports.isLessThanVersion = isLessThanVersion;