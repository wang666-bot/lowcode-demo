import { CalendarProps } from '../../calendar';
import { TimePickerProps } from '../../time-picker';
import BasePicker, { BasePickerProps } from './base-picker';
export interface DatePickerProps extends BasePickerProps {
    showTime?: boolean | TimePickerProps;
    dateCellRender: CalendarProps['dateCellRender'];
}
export default class DatePicker extends BasePicker<DatePickerProps> {
    get timeFormat(): string;
    get format(): string;
    renderPicker(): any;
    getDefaultFormat(): string;
}
