import type { PresetValidator, Rule, Validator } from './types';
/**
 * {required, format} => format; {required} => required
 * If a promise is wanted from the validator, either return a promise from the callback,
 *      or do not pass a callback
 */
export declare function validateFunc(validator: PresetValidator, ruleType: string): Validator;
/**
 * {required, format} => format; {required} => required
 */
export declare function getValidationMethod(rule: Rule): Validator | null;
