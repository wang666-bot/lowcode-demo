"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useFlatOptions = void 0;
var useFlatOptions = function (tree) {
    var flatData = function (data) {
        var list = [];
        data === null || data === void 0 ? void 0 : data.forEach(function (item) {
            var _a;
            list = __spreadArray(__spreadArray([], __read(list), false), [item], false);
            if ((_a = item === null || item === void 0 ? void 0 : item.children) === null || _a === void 0 ? void 0 : _a.length) {
                list = __spreadArray(__spreadArray([], __read(list), false), __read(flatData(item.children)), false);
            }
        });
        return list;
    };
    return flatData(tree);
};
exports.useFlatOptions = useFlatOptions;
//# sourceMappingURL=useFlatOptions.js.map