var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { connect, mapProps, mapReadPretty } from '@formily/react';
import { TimePicker2 as NextTimePicker2 } from '@alifd/next';
import { PreviewText } from '../preview-text';
import { formatMomentValue, momentable, mapSize, mapStatus, } from '../__builtins__';
var mapTimeFormat = function () {
    return function (props) {
        var format = props['format'] || 'HH:mm:ss';
        var onChange = props.onChange;
        return __assign(__assign({}, props), { format: format, value: momentable(props.value, format), onChange: function (value) {
                if (onChange) {
                    onChange(formatMomentValue(value, format));
                }
            } });
    };
};
export var TimePicker2 = connect(NextTimePicker2, mapProps(mapTimeFormat(), mapSize, mapStatus), mapReadPretty(PreviewText.TimePicker2));
TimePicker2.RangePicker = connect(NextTimePicker2.RangePicker, mapProps(mapTimeFormat(), mapSize, mapStatus), mapReadPretty(PreviewText.TimeRangePicker2));
export default TimePicker2;
//# sourceMappingURL=index.js.map